@model IEnumerable<Bigschool.Models.Following>

@{
    ViewBag.Title = "Following";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Following</h2>

<table class="table">
 

@foreach (var item in Model) {
    <tr>
        <td>
            * @item.Followee.Name
        </td>
    </tr>
}

</table>
